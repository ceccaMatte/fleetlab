# Docs

Reserved for repository documentation and operational notes.

## Local Backend Commands

- `npm run api:dev`: start the backend in watch mode
- `npm run api:start`: start the backend once
- `npm run api:check`: run TypeScript type-checking for the backend
- `npm run api:test`: run backend tests
